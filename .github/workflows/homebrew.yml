name: Homebrew head formula

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "CMakeLists.txt"
      - "src/**"
      - "resources/**"
      - "docs/**"
      - "packaging/homebrew/**"
      - ".github/workflows/homebrew.yml"

permissions:
  contents: read

jobs:
  install-test:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v7
      - name: Formula style
        run: brew style packaging/homebrew/pkmn-cli.rb
      - name: Install from public HEAD
        run: |
          brew tap AAAMAQ/pkmn
          brew install --HEAD --build-from-source AAAMAQ/pkmn/pkmn-cli
      - name: Formula test
        run: brew test AAAMAQ/pkmn/pkmn-cli
      - name: Installed deep smoke test
        run: |
          pkmn --version
          pkmn doctor --deep
